{%- liquid
  # The level is a setting so the page keeps one heading outline, but only h1 to h6 pass
  # through - anything else would put an arbitrary tag name straight from a setting.
  assign heading_tag = section.settings.heading_tag | default: 'h3'
  unless heading_tag == 'h1' or heading_tag == 'h2' or heading_tag == 'h3' or heading_tag == 'h4' or heading_tag == 'h5' or heading_tag == 'h6'
    assign heading_tag = 'h3'
  endunless
  # Ends the size chain: an explicit px value wins, then the typography preset, then the
  # size that belongs to the chosen level. It has to be published as a variable because a
  # class rule outranks the theme's `h3` selector, and an invalid var() resets to inherit
  # rather than falling through to it.
  assign heading_level_size = '--font-' | append: heading_tag | append: '--size'
  assign settings = section.settings
  assign body_preset = settings.body_type_preset | default: 'custom'
  assign has_media = false
  if settings.background_media == 'video' and settings.background_video != blank
    assign has_media = true
  elsif settings.background_media == 'image' and settings.background_image != blank
    assign has_media = true
  endif
-%}

{% render 'text-block-styles' %}

<div
  class="club-invite spacing-style"
  style="
    --section-heading-level-size: var({{ heading_level_size }});
    --club-invite-color: {{ settings.text_color }};
    --club-invite-overlay: rgba({{ settings.overlay_color.rgb }} / {{ settings.overlay_opacity | divided_by: 100.0 }});
    --club-invite-min-height: {{ settings.section_height }};
    --club-invite-button-background: {{ settings.button_background }};
    --club-invite-button-color: {{ settings.button_text }};
    --club-invite-body-width: {{ settings.body_width | default: '100%' }};
    --club-invite-body-alignment: {{ settings.body_alignment | default: 'center' }};
    {%- if settings.body_max_width == 'none' %}
      --club-invite-body-max-width: 100%;
    {%- else %}
      --club-invite-body-max-width: var(--max-width--body-{{ settings.body_max_width | default: 'normal' }});
    {%- endif %}
    {%- render 'spacing-style', settings: settings %}
  "
  {{ section.shopify_attributes }}
>
  {%- if has_media -%}
    <div class="club-invite__media">
      {%- render 'background-media',
        background_media: settings.background_media,
        background_video: settings.background_video,
        background_video_position: 'cover',
        background_image: settings.background_image,
        background_image_position: 'cover',
        section: section
      -%}
    </div>
  {%- endif -%}

  <div
    class="club-invite__overlay"
    aria-hidden="true"
  ></div>

  <div class="club-invite__content">
    <div class="club-invite__lockup">
      {%- if settings.badge != blank -%}
        {{
          settings.badge
          | image_url: width: 400
          | image_tag: widths: '120, 200, 300, 400', sizes: '160px', loading: 'lazy', class: 'club-invite__badge'
        }}
      {%- endif -%}

      {%- if settings.heading != blank -%}
        <{{ heading_tag }}
      class="club-invite__heading"
      style="{%- render 'typography-style', settings: section.settings, type: 'heading' -%}"
    >{{ settings.heading }}</{{ heading_tag }}>
      {%- endif -%}
    </div>

    {%- if settings.body != blank -%}
      <div
        class="club-invite__body text-block custom-color rte {{ body_preset }}{% if body_preset == 'custom' %} custom-typography custom-font-size{% endif %}"
        style="
          {%- render 'club-invite-typography-style', settings: settings, prefix: 'body', type: 'body' -%}
          --color: {{ settings.body_text_color | default: settings.text_color }};
        "
      >{{ settings.body }}</div>
    {%- endif -%}

    {%- if section.blocks.size > 0 -%}
      <ul class="club-invite__features">
        {%- for block in section.blocks -%}
          {%- if block.settings.label != blank -%}
            <li
              class="club-invite__feature"
              {{ block.shopify_attributes }}
            >
              {{- block.settings.label -}}
            </li>
          {%- endif -%}
        {%- endfor -%}
      </ul>
    {%- endif -%}

    {%- if settings.button_label != blank -%}
      <a
        class="button club-invite__button"
        {% if settings.button_link != blank %}
          href="{{ settings.button_link }}"
        {% endif %}
      >
        {{- settings.button_label -}}
      </a>
    {%- endif -%}
  </div>
</div>

{% stylesheet %}
  .club-invite {
    position: relative;
    display: grid;
    align-items: center;
    justify-items: center;
    min-block-size: var(--club-invite-min-height, 80svh);
    padding-inline: var(--page-margin, 1rem);
    color: var(--club-invite-color, var(--color-background));
    overflow: hidden;
    isolation: isolate;
  }

  .club-invite__media,
  .club-invite__overlay {
    position: absolute;
    inset: 0;
    z-index: -1;
  }

  .club-invite__media :is(img, video) {
    inline-size: 100%;
    block-size: 100%;
    object-fit: cover;
  }

  .club-invite__overlay {
    background: var(--club-invite-overlay);
  }

  .club-invite__content {
    display: grid;
    justify-items: center;
    gap: var(--gap-xl);
    text-align: center;
    max-inline-size: 46rem;
  }

  /* The badge sits beside the heading and tucks under it, as in the design. */
  .club-invite__lockup {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .club-invite__badge {
    inline-size: clamp(5rem, 10vw, 8rem);
    block-size: auto;
    margin-inline-end: -1.5rem;
    flex: none;
  }

  .club-invite__heading {
    margin: 0;
    font-family: var(--font-heading--family);
    font-size: var(--club-invite-heading-size, var(--font-size, var(--section-heading-level-size)));
    font-weight: var(--font-heading--weight);
  }

  .club-invite__body {
    display: grid;
    gap: var(--gap-sm);
    inline-size: var(--club-invite-body-width, 100%);
    max-inline-size: min(100%, var(--club-invite-body-max-width, var(--max-width--body-normal)));
    text-align: var(--club-invite-body-alignment, center);
  }

  .club-invite__body :is(p, ul, ol) {
    margin: 0;
    color: inherit;
    text-align: inherit;
  }

  .club-invite__features {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: var(--gap-lg);
    margin: 0;
    padding: var(--padding-sm) var(--padding-2xl);
    list-style: none;
    border: 1px solid currentcolor;
    border-radius: 999px;
    font-size: var(--font-size--sm);
  }

  /* An em dash between items, drawn rather than typed so it never lands at the end of a wrap. */
  .club-invite__feature + .club-invite__feature::before {
    content: '';
    display: inline-block;
    inline-size: 1.5rem;
    block-size: 1px;
    margin-inline-end: var(--gap-lg);
    vertical-align: middle;
    background: currentcolor;
  }

  .club-invite__button.button {
    --button-background-color: var(--club-invite-button-background);
    --button-color: var(--club-invite-button-color);

    justify-content: center;
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:names.club_invite",
  "tag": "section",
  "blocks": [
    {
      "type": "feature",
      "name": "t:names.club_invite_feature",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "t:settings.label",
          "default": "Freshly roasted"
        }
      ]
    }
  ],
  "max_blocks": 5,
  "settings": [
    {
      "type": "select",
      "id": "heading_tag",
      "label": "t:settings.heading_tag",
      "info": "t:info.heading_tag",
      "options": [
        {
          "value": "h1",
          "label": "H1"
        },
        {
          "value": "h2",
          "label": "H2"
        },
        {
          "value": "h3",
          "label": "H3"
        },
        {
          "value": "h4",
          "label": "H4"
        },
        {
          "value": "h5",
          "label": "H5"
        },
        {
          "value": "h6",
          "label": "H6"
        }
      ],
      "default": "h3"
    },
    {
      "type": "image_picker",
      "id": "badge",
      "label": "t:settings.image",
      "info": "t:info.club_invite_badge"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "t:settings.heading",
      "default": "Join the <em>club</em>"
    },
    {
      "type": "header",
      "content": "t:content.typography"
    },
    {
      "type": "select",
      "id": "type_preset",
      "label": "t:settings.preset",
      "options": [
        {
          "value": "rte",
          "label": "t:options.default"
        },
        {
          "value": "paragraph",
          "label": "t:options.paragraph"
        },
        {
          "value": "h1",
          "label": "t:options.h1"
        },
        {
          "value": "h2",
          "label": "t:options.h2"
        },
        {
          "value": "h3",
          "label": "t:options.h3"
        },
        {
          "value": "h4",
          "label": "t:options.h4"
        },
        {
          "value": "h5",
          "label": "t:options.h5"
        },
        {
          "value": "h6",
          "label": "t:options.h6"
        },
        {
          "value": "custom",
          "label": "t:options.custom"
        }
      ],
      "default": "rte",
      "info": "t:info.edit_presets_in_theme_settings"
    },
    {
      "type": "select",
      "id": "font",
      "label": "t:settings.font",
      "options": [
        {
          "value": "var(--font-body--family)",
          "label": "t:options.body"
        },
        {
          "value": "var(--font-subheading--family)",
          "label": "t:options.subheading"
        },
        {
          "value": "var(--font-heading--family)",
          "label": "t:options.heading"
        },
        {
          "value": "var(--font-accent--family)",
          "label": "t:options.accent"
        }
      ],
      "default": "var(--font-body--family)",
      "visible_if": "{{ section.settings.type_preset == 'custom' }}"
    },
    {
      "type": "select",
      "id": "font_size",
      "label": "t:settings.size",
      "options": [
        {
          "value": "",
          "label": "t:options.default"
        },
        {
          "value": "0.625rem",
          "label": "10px"
        },
        {
          "value": "0.75rem",
          "label": "12px"
        },
        {
          "value": "0.875rem",
          "label": "14px"
        },
        {
          "value": "1rem",
          "label": "16px"
        },
        {
          "value": "1.125rem",
          "label": "18px"
        },
        {
          "value": "1.25rem",
          "label": "20px"
        },
        {
          "value": "1.5rem",
          "label": "24px"
        },
        {
          "value": "2rem",
          "label": "32px"
        },
        {
          "value": "2.5rem",
          "label": "40px"
        },
        {
          "value": "3rem",
          "label": "48px"
        },
        {
          "value": "3.5rem",
          "label": "56px"
        },
        {
          "value": "4.5rem",
          "label": "72px"
        },
        {
          "value": "5.5rem",
          "label": "88px"
        },
        {
          "value": "7.5rem",
          "label": "120px"
        },
        {
          "value": "9.5rem",
          "label": "152px"
        },
        {
          "value": "11.5rem",
          "label": "184px"
        }
      ],
      "default": "1rem",
      "visible_if": "{{ section.settings.type_preset == 'custom' }}"
    },
    {
      "type": "select",
      "id": "line_height",
      "label": "t:settings.line_height",
      "options": [
        {
          "value": "tight",
          "label": "t:options.tight"
        },
        {
          "value": "normal",
          "label": "t:options.normal"
        },
        {
          "value": "loose",
          "label": "t:options.loose"
        }
      ],
      "default": "normal",
      "visible_if": "{{ section.settings.type_preset == 'custom' }}"
    },
    {
      "type": "select",
      "id": "letter_spacing",
      "label": "t:settings.letter_spacing",
      "options": [
        {
          "value": "tight",
          "label": "t:options.tight"
        },
        {
          "value": "normal",
          "label": "t:options.normal"
        },
        {
          "value": "loose",
          "label": "t:options.loose"
        }
      ],
      "default": "normal",
      "visible_if": "{{ section.settings.type_preset == 'custom' }}"
    },
    {
      "type": "select",
      "id": "case",
      "label": "t:settings.case",
      "options": [
        {
          "value": "none",
          "label": "t:options.default"
        },
        {
          "value": "uppercase",
          "label": "t:options.uppercase"
        }
      ],
      "default": "none",
      "visible_if": "{{ section.settings.type_preset == 'custom' }}"
    },
    {
      "type": "select",
      "id": "wrap",
      "label": "t:settings.wrap",
      "options": [
        {
          "value": "pretty",
          "label": "t:options.pretty"
        },
        {
          "value": "balance",
          "label": "t:options.balance"
        },
        {
          "value": "nowrap",
          "label": "t:options.none"
        }
      ],
      "visible_if": "{{ section.settings.type_preset == 'custom' }}"
    },
    {
      "type": "richtext",
      "id": "body",
      "label": "t:settings.description"
    },
    {
      "type": "header",
      "content": "t:content.layout"
    },
    {
      "type": "select",
      "id": "body_width",
      "label": "t:settings.width",
      "options": [
        {
          "value": "fit-content",
          "label": "t:options.fit"
        },
        {
          "value": "100%",
          "label": "t:options.fill"
        }
      ],
      "default": "100%"
    },
    {
      "type": "select",
      "id": "body_max_width",
      "label": "t:settings.max_width",
      "options": [
        {
          "value": "narrow",
          "label": "t:options.narrow"
        },
        {
          "value": "normal",
          "label": "t:options.normal"
        },
        {
          "value": "none",
          "label": "t:options.none"
        }
      ],
      "default": "normal"
    },
    {
      "type": "text_alignment",
      "id": "body_alignment",
      "label": "t:settings.alignment",
      "default": "center"
    },
    {
      "type": "header",
      "content": "t:content.typography"
    },
    {
      "type": "select",
      "id": "body_type_preset",
      "label": "t:settings.preset",
      "options": [
        { "value": "rte", "label": "t:options.default" },
        { "value": "paragraph", "label": "t:options.paragraph" },
        { "value": "h1", "label": "t:options.h1" },
        { "value": "h2", "label": "t:options.h2" },
        { "value": "h3", "label": "t:options.h3" },
        { "value": "h4", "label": "t:options.h4" },
        { "value": "h5", "label": "t:options.h5" },
        { "value": "h6", "label": "t:options.h6" },
        { "value": "custom", "label": "t:options.custom" }
      ],
      "default": "custom",
      "info": "t:info.edit_presets_in_theme_settings"
    },
    {
      "type": "select",
      "id": "body_font",
      "label": "t:settings.font",
      "options": [
        { "value": "var(--font-body--family)", "label": "t:options.body" },
        { "value": "var(--font-subheading--family)", "label": "t:options.subheading" },
        { "value": "var(--font-heading--family)", "label": "t:options.heading" },
        { "value": "var(--font-accent--family)", "label": "t:options.accent" }
      ],
      "default": "var(--font-body--family)",
      "visible_if": "{{ section.settings.body_type_preset == 'custom' }}"
    },
    {
      "type": "select",
      "id": "body_font_size",
      "label": "t:settings.size",
      "options": [
        { "value": "", "label": "t:options.default" },
        { "value": "0.625rem", "label": "10px" },
        { "value": "0.75rem", "label": "12px" },
        { "value": "0.875rem", "label": "14px" },
        { "value": "1rem", "label": "16px" },
        { "value": "1.125rem", "label": "18px" },
        { "value": "1.25rem", "label": "20px" },
        { "value": "1.5rem", "label": "24px" },
        { "value": "2rem", "label": "32px" },
        { "value": "2.5rem", "label": "40px" },
        { "value": "3rem", "label": "48px" },
        { "value": "3.5rem", "label": "56px" },
        { "value": "4.5rem", "label": "72px" },
        { "value": "5.5rem", "label": "88px" },
        { "value": "7.5rem", "label": "120px" },
        { "value": "9.5rem", "label": "152px" },
        { "value": "11.5rem", "label": "184px" }
      ],
      "default": "1.125rem",
      "visible_if": "{{ section.settings.body_type_preset == 'custom' }}"
    },
    {
      "type": "select",
      "id": "body_line_height",
      "label": "t:settings.line_height",
      "options": [
        { "value": "tight", "label": "t:options.tight" },
        { "value": "normal", "label": "t:options.normal" },
        { "value": "loose", "label": "t:options.loose" }
      ],
      "default": "normal",
      "visible_if": "{{ section.settings.body_type_preset == 'custom' }}"
    },
    {
      "type": "select",
      "id": "body_letter_spacing",
      "label": "t:settings.letter_spacing",
      "options": [
        { "value": "tight", "label": "t:options.tight" },
        { "value": "normal", "label": "t:options.normal" },
        { "value": "loose", "label": "t:options.loose" }
      ],
      "default": "normal",
      "visible_if": "{{ section.settings.body_type_preset == 'custom' }}"
    },
    {
      "type": "select",
      "id": "body_case",
      "label": "t:settings.case",
      "options": [
        { "value": "none", "label": "t:options.default" },
        { "value": "uppercase", "label": "t:options.uppercase" }
      ],
      "default": "none",
      "visible_if": "{{ section.settings.body_type_preset == 'custom' }}"
    },
    {
      "type": "select",
      "id": "body_wrap",
      "label": "t:settings.wrap",
      "options": [
        { "value": "pretty", "label": "t:options.pretty" },
        { "value": "balance", "label": "t:options.balance" },
        { "value": "nowrap", "label": "t:options.none" }
      ],
      "default": "pretty",
      "visible_if": "{{ section.settings.body_type_preset == 'custom' }}"
    },
    {
      "type": "color",
      "id": "body_text_color",
      "label": "t:settings.text_color",
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "t:content.buttons"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "t:settings.label",
      "default": "Explore subscriptions"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "t:settings.link"
    },
    {
      "type": "color",
      "id": "button_background",
      "label": "t:settings.background",
      "default": "#751132"
    },
    {
      "type": "color",
      "id": "button_text",
      "label": "t:settings.text",
      "default": "#f5f4ea"
    },
    {
      "type": "header",
      "content": "t:content.media"
    },
    {
      "type": "select",
      "id": "background_media",
      "label": "t:settings.media_type",
      "options": [
        {
          "value": "none",
          "label": "t:options.none"
        },
        {
          "value": "image",
          "label": "t:options.image"
        },
        {
          "value": "video",
          "label": "t:options.video"
        }
      ],
      "default": "image"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "t:settings.image",
      "visible_if": "{{ section.settings.background_media == 'image' }}"
    },
    {
      "type": "video",
      "id": "background_video",
      "label": "t:settings.video",
      "visible_if": "{{ section.settings.background_media == 'video' }}"
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "t:settings.overlay_color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "t:settings.opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "default": 35
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "t:settings.text_color",
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "t:content.layout"
    },
    {
      "type": "select",
      "id": "section_height",
      "label": "t:settings.height",
      "options": [
        {
          "value": "auto",
          "label": "t:options.auto"
        },
        {
          "value": "60svh",
          "label": "t:options.small"
        },
        {
          "value": "80svh",
          "label": "t:options.medium"
        },
        {
          "value": "100svh",
          "label": "t:options.full_screen"
        }
      ],
      "default": "80svh"
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 80
    }
  ],
  "presets": [
    {
      "name": "t:names.club_invite",
      "blocks": [
        {
          "type": "feature"
        },
        {
          "type": "feature"
        },
        {
          "type": "feature"
        }
      ]
    }
  ]
}
{% endschema %}
